---
# Handlers for opennebula-node role
# NOTE: Uses raw commands for Tailscale SSH compatibility

- name: Restart libvirtd
  ansible.builtin.raw: |
    systemctl restart libvirtd
    systemctl is-active libvirtd
